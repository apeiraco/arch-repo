# Configuration for OpenVSCode Server
# These arguments are passed directly to the openvscode-server executable.
# Example: OPENVSCODE_ARGS="--host 127.0.0.1 --port 3000 --without-connection-token"
OPENVSCODE_ARGS="--host 127.0.0.1 --port 3000"
